{
  "private": true,
  "scripts": {
    "test": "mocha -r ts-node/register **/test/**/*.spec.ts **/src/**/*.spec.ts",
    "prepare": "yarn tslint && lerna run prepare",
    "rebuild:browser": "theia rebuild:browser",
    "rebuild:electron": "theia rebuild:electron",
    "tslint-fix": "tslint --fix --project ssh-extension",
    "tslint": "tslint --project ssh-extension"
  },
  "license": "EPL-2.0",
  "devDependencies": {
    "lerna": "2.4.0",
    "mocha": "5.0.2",
    "tslint": "5.10.0"
  },
  "workspaces": [
    "ssh-extension",
    "browser-app",
    "electron-app"
  ]
}
